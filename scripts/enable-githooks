#!/bin/sh

SRC=$(pwd)/.githooks/pre-commit
DST=$(pwd)/.git/hooks/pre-commit

if [ ! -f $SRC ]; then
    echo "$SRC not found, you need to be in the top-level directory"
    exit 1
fi

ln -s $SRC $DST
